---
'name': PismoID
---

```
<Playground>
  {() => {
    const AppAuth = Auth({
      baseURL: 'URL',
      onUpdate: (...args) => console.log(args)
    })
    return (
      <Provider>
        <PismoID auth={AppAuth}>
          <div>LOGADO!!!</div>
        </PismoID>
      </Provider>
    )
  }}
</Playground>
```
