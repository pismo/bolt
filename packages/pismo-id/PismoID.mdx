---
'name': PismoID
---

```
<Playground>
  {() => {
    const AppAuth = Auth({
      baseURL: 'URL',
      onUpdate: (...args) => console.log(args)
    })
    return (
      <Bolt>
        <PismoID auth={AppAuth}>
          <div>LOGADO!!!</div>
        </PismoID>
      </Bolt>
    )
  }}
</Playground>
```
