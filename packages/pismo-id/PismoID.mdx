---
'name': PismoID
---

```
<Playground>
  {() => {
    const AppAuth = Auth({
      baseURL: 'https://api-sandbox.pismolabs.io',
      onUpdate: (...args) => console.log(args)
    })
    return (
      <Provider>
        <PismoID auth={AppAuth}>
          <div>LOGADO!!!</div>
        </PismoID>
      </Provider>
    )
  }}
</Playground>
```
