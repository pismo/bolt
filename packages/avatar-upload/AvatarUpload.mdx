---
'name': AvatarUpload
---

import { useState, useRef, useEffect } from 'react'
import { Playground } from 'docz'
import { Bolt } from '@pismo/bolt-core'
import { AvatarUpload } from './AvatarUpload'
import Box from '@material-ui/core/Box'
import Button from '@material-ui/core/Button'

# AvatarUpload

<Playground>
    {
        () => {
            const [result, setResult] = useState(null)
            const [getResult, setGetResult] = useState(null)

            const imgDone = (_getResult) => {
                console.log(_getResult)
                setGetResult(_getResult)
            }

            const clicked = () => {
                console.log(getResult)
                if(getResult) {
                    console.log(getResult)
                    getResult({type: 'html'}).then(img => setResult(img))                    
                }
            }

            return (
                <Bolt>
                    <Box display='flex' width={1}>
                        <Box width={1/3}><AvatarUpload buttonLabel='Change profile image' imageUploaded={imgDone} resultType='html' /></Box>
                        <Box width={1/3}><Button variant='contained' color='primary' onClick={clicked}>show result</Button></Box>
                        <Box width={1/3}>{result}</Box>
                    </Box>
                </Bolt>
            )
        }
    }

</Playground>
